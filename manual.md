# SellerKit 用户手册

> 版本 1.0.0 | 跨平台桌面应用 | 适用于 macOS / Windows

---

## 目录

1. [产品介绍](#一产品介绍)
2. [安装与运行](#二安装与运行)
3. [快速上手](#三快速上手)
4. [界面布局](#四界面布局)
5. [功能详解](#五功能详解)
   - [5.1 导入 Excel 数据](#51-导入-excel-数据)
   - [5.2 查看与选择记录](#52-查看与选择记录)
   - [5.3 搜索与筛选](#53-搜索与筛选)
   - [5.4 复制到剪贴板](#54-复制到剪贴板)
   - [5.5 撤销与重置](#55-撤销与重置)
   - [5.6 模板管理](#56-模板管理)
   - [5.7 内容预览](#57-内容预览)
6. [快捷键一览](#六快捷键一览)
7. [常见问题](#七常见问题)

---

## 一、产品介绍

**SellerKit** 是一款轻量、开箱即用的桌面工具，专为需要**批量数据 + 模板填充 + 逐条复制**的场景设计。

### 典型使用场景

- 电商运营：从 Excel 导出商品数据，用模板生成商品文案
- 客户服务：批量替换模板中的客户名、订单号等信息，逐条复制回复
- 内容生产：用结构化数据驱动文案生成

### 核心特性

| 特性 | 说明 |
|---|---|
| 📂 多种导入方式 | 按钮选取、拖拽文件、自动恢复上次会话 |
| 🧠 智能解析 | 自动识别表头、跳过空行、处理重复列名 |
| 📝 灵活模板 | 支持多模板管理、占位符自动补全、实时预览 |
| ⚡ 一键复制 | 复制替换结果到剪贴板，自动标记已使用 |
| ↩️ 撤销/重置 | 支持撤销操作，一键重置所有使用状态 |
| 🔍 搜索筛选 | 按关键词搜索，按使用状态筛选 |
| 💾 自动保存 | 状态自动持久化，重启后恢复 |

---

## 二、安装与运行

### 2.1 下载安装包

从发布页面下载对应平台的安装包：

| 平台 | 文件格式 |
|---|---|
| macOS | `SellerKit-1.0.0-mac-arm64.dmg` |
| Windows | `SellerKit-1.0.0-win-x64.exe` |

> 应用签名信息：当前版本为 ad-hoc 签名，macOS 上首次打开可能提示无法验证开发者。请右键点击应用 → 选择"打开"即可运行。

### 2.2 从源码运行（开发者）

```bash
# 克隆项目
cd iptv

# 安装依赖
npm install

# 开发模式启动
npm run dev

# 打包为可分发版本
npm run dist:mac    # macOS
npm run dist:win    # Windows
```

### 2.3 系统要求

- **macOS**: 11.0 (Big Sur) 及以上，Apple Silicon (arm64)
- **Windows**: Windows 10 及以上，64 位

---

## 三、快速上手

### 3 分钟学会使用

**第一步：导入 Excel 数据**

启动应用后，将包含数据的 `.xlsx` 文件直接拖入左侧区域，或点击工具栏的 **「导入Excel」** 按钮选择文件。

**第二步：准备模板**

点击工具栏的 **「模板配置」** 按钮，在面板中：
- 点击 **「导入模板文件」** 选择一个 `.txt` 模板文件
- 或点击 **「新建空白模板」** 手动编写模板内容

模板中使用 `{{列名}}` 作为占位符，例如：

```
尊敬的 {{客户姓名}}，您的订单 {{订单号}} 已发货，预计 {{送达日期}} 送达。
```

**第三步：复制内容**

- 点击左侧列表中的记录，右侧预览替换后的内容
- 点击 **「复制并标记已使用」** 按钮，或直接按 `Enter` 键
- 内容已复制到剪贴板，直接粘贴使用即可

---

## 四、界面布局

```
 ┌─────────────────────────────────────────────────────────────┐
 │  搜索框  [全部(10)] [未使用(7)] [已使用(3)]  导入Excel  模板配置  ↩  重置 │  ← 工具栏
 ├──────────────────────────┬──────────────────────────────────┤
 │                          │                                  │
 │   ○ 姓名:张三  电话:138.. │   内容预览                       │
 │   ● 姓名:李四  电话:139.. │   ┌──────────────────────────┐   │
 │   ○ 姓名:王五  电话:137.. │   │ 尊敬的张三，您的订单...    │   │  ← 预览面板
 │   ○ 姓名:赵六  电话:136.. │   └──────────────────────────┘   │      (右侧 40%)
 │                          │                                  │
 │   ○ 姓名:孙七  电话:135.. │   字段详情 ▾                    │
 │                          │   姓名: 张三                     │
 │  ← 记录列表              │   电话: 138xxxx                  │
 │     (左侧 60%)           │   订单号: ORD-2024-001           │
 │                          │   ─────────────────────          │
 │                          │   [复制并标记已使用]              │
 └──────────────────────────┴──────────────────────────────────┘
```

### 界面区域说明

| 区域 | 说明 |
|---|---|
| **工具栏** | 位于顶部，包含搜索框、筛选按钮、操作按钮 |
| **记录列表** | 左侧区域，展示所有数据记录，支持拖拽导入 |
| **预览面板** | 右侧区域，展示选中记录的模板替换结果 |
| **模板管理** | 侧边抽屉面板，点击「模板配置」按钮打开 |

---

## 五、功能详解

### 5.1 导入 Excel 数据

应用支持 **3 种导入方式**：

#### 方式一：拖拽导入
- 将 `.xlsx` 文件从文件夹拖入左侧区域
- 拖入时区域边缘会高亮显示
- 仅支持 `.xlsx` 和 `.xls` 格式

#### 方式二：按钮选取
- 点击工具栏的 **「导入Excel」** 按钮
- 在弹出的文件对话框中选择 Excel 文件

#### 方式三：自动恢复
- 应用关闭时会自动保存当前状态
- 下次启动时，如果上次的 Excel 文件仍然存在，自动加载

#### 智能解析规则
- 自动跳过文件首尾的空白行
- 将第一个非空行识别为表头（列名）
- 如果表头有重复列名，自动添加后缀区分（如 `姓名`、`姓名_1`）
- 自动跳过数据行中的空行
- 如果重新导入同一个文件，保持之前已使用/未使用的标记状态

---

### 5.2 查看与选择记录

- 每条记录左侧显示状态图标：
  - **绿色圆点** `●` — 已使用
  - **灰色圆圈** `○` — 未使用
- 每条记录显示前两个字段的值，方便快速识别
- 点击记录选中，选中项高亮蓝色边框
- 已使用的记录显示为**灰色半透明 + 删除线**
- 已使用的记录自动排列到列表末尾

---

### 5.3 搜索与筛选

#### 搜索
- 在工具栏搜索框中输入关键词
- 系统自动匹配所有字段，不区分大小写
- 搜索时列表实时过滤，只显示匹配的记录

#### 筛选
- **全部** — 显示所有记录
- **未使用** — 仅显示未使用的记录
- **已使用** — 仅显示已使用的记录
- 每个按钮旁边显示对应数量

---

### 5.4 复制到剪贴板

#### 操作方法
- **双击**列表中的记录
- 选中记录后，点击右侧面板的 **「复制并标记已使用」** 按钮
- 选中记录后，按 `Enter` 键

#### 复制逻辑
- **未使用的记录**：复制替换后的文本到剪贴板 → 自动标记为已使用 → 移动到已使用分组
- **已使用的记录**：仅复制文本到剪贴板，不改变状态（按钮显示为「仅复制」）

#### 复制结果格式
复制到剪贴板的是纯文本内容，模板中的 `{{列名}}` 占位符会被替换为对应字段的值。例如：
```
原始模板:  尊敬的 {{姓名}}，您的订单 {{订单号}} 已处理。
复制结果:  尊敬的 张三，您的订单 ORD-2024-001 已处理。
```

---

### 5.5 撤销与重置

#### 撤销（Undo）
- **用途**：撤销最近一次的"复制并标记已使用"操作
- **操作方式**：
  - 点击工具栏的 **↩ 撤销** 按钮
  - 按快捷键 `Ctrl+Z` (Windows) 或 `Cmd+Z` (macOS)
- 撤销后，记录恢复为未使用状态，回到原来的排序位置
- 可以连续撤销多次

#### 重置（Reset）
- **用途**：将所有记录重置为未使用状态
- **操作方式**：点击工具栏的红色 **「重置」** 按钮
- 重置前会弹出确认对话框，防止误操作
- 重置后所有记录回到初始排序

---

### 5.6 模板管理

点击工具栏的 **「模板配置」** 按钮，打开侧边抽屉面板。

#### 界面说明

```
 ┌──────────────────────────────────────────┐
 │ 模板配置                           ✕     │  ← 标题栏
 ├──────────────┬───────────────────────────┤
 │ 模板列表      │  模板名称: [默认模板    ]  │
 │              │                           │
 │ ● 默认模板   │  设为当前                   │
 │ ○ 商品模板   │  ┌───────────────────────┐ │
 │   (文件丢失)  │  │ 尊敬的{{姓名}}，       │ │  ← 模板编辑器
 │              │  │ 您的订单{{订单号}}      │ │     (支持占位符)
 │              │  └───────────────────────┘ │
 │              │                           │
 │              │  [保存]    [删除]          │
 │              │           占位符: {{列名}}  │
 ├──────────────┤                           │
 │ + 导入模板   │                           │
 │ + 新建空白   │                           │
 └──────────────┴───────────────────────────┘
```

#### 管理模板

| 操作 | 方法 |
|---|---|
| **导入模板文件** | 点击「导入模板文件」，选择 `.txt` 文件 |
| **新建空白模板** | 点击「新建空白模板」，手动输入模板内容 |
| **编辑模板** | 点击左侧模板列表中的模板，在右侧编辑区域修改 |
| **切换当前模板** | 选中模板后点击「设为当前」，或直接点击左侧列表切换 |
| **删除模板** | 选中非当前模板后点击「删除」（默认模板不可删除） |
| **保存模板** | 编辑后点击「保存」，内容会写入原文件并持久化存储 |

#### 使用占位符

在模板文本中使用 `{{列名}}` 表示占位符，例如：

```
{{客户姓名}} 您好，

您的退货申请（编号：{{退货单号}}）已收到。
我们将在 {{处理天数}} 个工作日内为您处理。

感谢您的耐心等待！
{{店铺名称}}
```

#### 智能补全

在模板编辑器中输入 `{{` 后，系统会自动弹出可用列名列表（基于当前 Excel 的表头），点击即可插入，无需手动输入完整的列名。

---

### 5.7 内容预览

选中一条记录后，右侧预览面板会显示：

1. **模板替换结果** — 展示模板中的占位符被替换后的完整文本
   - 已替换的字段显示为**蓝色文本**
   - 如果某个字段缺失，显示为**红色标记** `[字段缺失: xxx]`
2. **字段详情** — 点击可展开，查看当前记录所有字段的键值对
3. **状态标签** — 显示"已使用"或"未使用"标签

如果当前没有选中任何记录，预览区会显示提示文字"点击左侧记录预览内容"。

---

## 六、快捷键一览

| 快捷键 | 功能 |
|---|---|
| `↑` (上箭头) | 选中上一条记录 |
| `↓` (下箭头) | 选中下一条记录 |
| `Enter` | 复制当前选中记录到剪贴板（并标记已使用） |
| `Ctrl+Z` / `Cmd+Z` | 撤销上次复制操作 |
| `Ctrl+Z` (macOS: `Cmd+Z`) | 撤销 |

> 模板管理器打开时，快捷键暂时禁用。

---

## 七、常见问题

### Q1：支持哪些 Excel 格式？

支持 `.xlsx` 和 `.xls` 格式。应用会读取第一个工作表的内容。

### Q2：模板文件支持什么格式？

支持 `.txt` 格式的纯文本文件。模板中使用 `{{列名}}` 作为占位符，列名需要与 Excel 的表头完全一致（包含空格和特殊字符）。

### Q3：为什么我导入后看不到数据？

可能原因：
- Excel 文件为空或没有有效数据行
- 第一行（表头行）全部为空
- 文件格式不支持（请使用 `.xlsx` 格式）

### Q4：已使用的记录还能复制吗？

可以。已使用的记录再次复制时，仅复制文本到剪贴板，不会改变使用状态。按钮会显示为「仅复制」。

### Q5：关闭应用后数据会丢失吗？

不会。应用会自动保存以下状态：
- 最近使用的 Excel 文件路径
- 所有记录的使用状态和排序
- 模板配置列表和当前选中模板
- 下次启动时自动恢复

### Q6：模板文件被外部删除了怎么办？

如果模板对应的文件被删除，模板列表中会显示"文件丢失"标记。您可以选择：
- 重新导入该模板的文件
- 编辑模板内容后保存（会创建新文件）
- 删除该模板

### Q7：如何重置所有数据？

点击工具栏的红色 **「重置」** 按钮，将所有记录恢复为未使用状态。重置前会弹出确认提示。

### Q8：应用窗口大小可以调整吗？

可以。窗口默认大小 1200×800，最小尺寸 900×600。您可以自由拖拽调整窗口大小。

---

*最后更新：2024 年*
